const names = ["Edoardo", "Simone", "Francesco"];

// Stampa in console tutti i nomi
// Risultato: 'Edoardo', 'Simone', 'Francesco'

// todo - soluzione

const namePeople = names.map((name) => name).join(`, `);
console.log(namePeople);
